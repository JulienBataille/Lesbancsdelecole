{% extends 'base.html.twig' %}

{% block body %}
    <div class="container my-3">
        <table class="table">
            <tr>
                <th>Id</th>
                <th>Quantité</th>
                <th>Date d'ajout</th>

            </tr>

            {% for productAdded in productsAdded %}
                <tr>
                    <th>{{productAdded.id}}</th>
                    <th>{{productAdded.qte}}</th>
                    <th>{{productAdded.createdAt | date('d M Y H:i')}}</th>
                </tr>
                
            {% endfor %}
        </table>
    </div>
    
{% endblock %}